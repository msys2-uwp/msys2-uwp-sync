{
  "name": "msys2-apiss-sync",
  "private": true,
  "packageManager": "yarn@4.17.0",
  "type": "module",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "sync": "node src/cli/sync-upstream.ts",
    "fetch-mirrors": "node src/cli/fetch-mirrors.ts",
    "retrieve-history": "node src/cli/retrieve-history.ts",
    "merge-queue": "node src/cli/merge-queue.ts",
    "prepare-benchmark": "node src/cli/prepare-benchmark.ts",
    "save-replay-graph": "node src/cli/prepare-benchmark.ts --graph-only",
    "fork-safe-benchmark": "node src/cli/fork-safe-benchmark.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
